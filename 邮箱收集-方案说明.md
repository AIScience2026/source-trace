# 邮箱收集方案 · 落地说明（2026-09-23）

## 一句话

**mailto 直连 SourceTrace 内测邮箱，零第三方表单、零后端、零你的 QQ 邮箱。**

## 为什么是这个方案

| 候选方案 | 结论 | 原因 |
|---|---|---|
| ~~金数据 / Formspree~~ | ❌ 排除 | 第三方表单服务，用户数据过别人服务器，你要求不用 |
| ~~QQ 邮箱 SMTP~~ | ❌ 排除 | 你明确说不用自己的 QQ 邮箱 |
| ~~FunctionGraph + APIG + SMTP 转发~~ | ⏸️ 暂缓 | 技术可行但要 15-20 分钟部署 + 4 个环境变量 + 你的授权码；当前阶段属于过度工程 |
| **mailto → agent 邮箱** | ✅ 采用 | 3 分钟上线、零基础设施、数据直接进我们自己的收件箱 |

## 技术事实（重要，别误解）

`agently-cli` 是**命令行邮件客户端**，不是公网 HTTP 服务。所以：

- GitHub Pages 是纯静态站，**无法**在服务端调用任何邮件 API
- 页面只能通过 `mailto:` 协议**唤起用户本地的邮件客户端**，而不是服务端代发
- 因此真实链路是：用户点「订阅通知」→ 浏览器唤起本地邮件客户端 → 预填收件人/主题/正文 → **用户点发送** → 邮件到达 `yuanting7971@agent.qq.com`

这不是 bug，是纯静态站点在「不用第三方」约束下的唯一解。

## 已验证的闭环

| 验证项 | 结果 |
|---|---|
| 线上页面已更新 | ✅ `https://aiscience2026.github.io/source-trace/` HTTP 200，含 `LEAD_EMAIL` |
| mailto 生成逻辑 | ✅ node --check 通过；URL 653 字符，远低于 2000 上限 |
| 发信链路实测 | ✅ 发送 → 收件箱收到 → 正文完整（含联系邮箱/时间/来源页） |
| 测试邮件清理 | ✅ 已删除 |

实测邮件内容（用户视角会看到的）：

```
收件人：yuanting7971@agent.qq.com
主题：SourceTrace 上线通知订阅

我要订阅 SourceTrace 内测上线通知。

我的联系邮箱：<用户输入的邮箱>
订阅时间：<本地时间>
来源页面：https://aiscience2026.github.io/source-trace/

（这封邮件由 SourceTrace 落地页自动生成，直接发送即可）
```

## 换邮箱地址怎么改

改 `index.html` 里这两处（都搜 `LEAD_EMAIL` / `yuanting7971@agent.qq.com`）：

1. JS：`const LEAD_EMAIL = 'yuanting7971@agent.qq.com';`
2. 页面说明文字里的 mailto 链接

然后 commit + push，等 GitHub Pages 重新构建（约 1-10 分钟）。

## 已知局限（诚实清单）

1. **需要用户多点一次「发送」**——mailto 不会自动发送。转化率会低于原地 POST。
2. **用户必须本地配了邮件客户端**（Outlook / QQ 邮箱 / 飞书 / 网页版邮箱都不行）。没配的用户看到的是唤起失败提示，页面已给出兜底文案「请手动发到 …」。
3. **邮箱只在邮件正文里，不在数据库里**——要看订阅列表，用：
   ```bash
   agently-cli message +search --q "上线通知订阅" --dir inbox
   ```
4. **limit 额度**：agently-cli 账户 `daily_send_quota: 50`、`requests_per_minute: 10`。这是**收件侧**的限制——用户是发信方、我们收信，所以不影响用户，但影响我们日后用这个邮箱群发回复。

## 如果以后要升级

触发条件：订阅量 > 30 人，或者开始需要自动回复 / API Key 发放。

升级路径：FunctionGraph 版代码和部署文档仍在仓库里（`functions/lead_collector/index.py`），届时把 `index.html` 的 `LEAD_EMAIL` 逻辑换回 `fetch(LEAD_ENDPOINT)` 即可，注释里留了痕迹。

## 相关文件

- `index.html` —— 落地页，第 259-269 行表单 + 第 373-418 行 mailto 逻辑
- `functions/lead_collector/index.py` —— 备用 SMTP 转发后端（未部署）
