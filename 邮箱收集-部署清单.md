# FunctionGraph 邮箱收集 — 部署清单

> 完成时间预计：30 分钟
> 目标：落地页的「订阅通知」原地提交，无跳转，数据落自己的 OBS

---

## 一、前置准备（10 分钟）

### 1. 建 OBS 桶（存邮箱数据）

1. 登录华为云控制台 → 搜索「对象存储 OBS」
2. 点「创建桶」：
   - **区域**：`华北-北京四`（cn-north-4，和你函数要同区域）
   - **桶名**：`source-trace-leads`（全局唯一，被占了就换）
   - **桶类型**：标准存储
   - **权限**：**私有**（重要，邮箱是敏感数据）
   - 其他默认
3. 创建后记住桶名和区域

### 2. 拿访问密钥（AK/SK）

1. 控制台右上角头像 →「我的凭证」→「访问密钥」
2. 「新增访问密钥」
3. 下载 CSV，里面的 `AK` 和 `SK` 保存好（只显示一次）

---

## 二、创建函数（10 分钟）

1. 控制台搜索「函数计算 FunctionGraph」
2. 「创建函数」：
   - **类型**：事件函数
   - **函数名**：`lead-collector`
   - **区域**：`cn-north-4`（**必须和 OBS 桶同区域**）
   - **运行时**：`Python 3.9`
   - **上传方式**：**代码在线编辑**
3. 创建后进入函数详情 →「代码」标签页
4. 把 `functions/lead_collector/index.py` 的**全部内容**粘贴进去
5. 「部署」

---

## 三、加 OBS SDK 依赖（3 分钟）

函数用到华为云 OBS Python SDK，需要加进去：

1. 函数详情 →「依赖」标签页（或「代码」页顶部的「依赖包」）
2. 点「添加依赖」或「编辑依赖」
3. 添加一行：`esdk-obs-python`
4. 保存，等安装完成（约 1 分钟）

> 如果控制台没有依赖管理界面，就把 `pip install esdk-obs-python` 的结果放本地，上传 zip 依赖包。多数情况下直接填这一行就行。

---

## 四、配环境变量（2 分钟）

函数详情 →「配置」→「环境变量」，添加：

| 键 | 值 |
|---|---|
| `LEADS_OBS_BUCKET` | `source-trace-leads`（你的桶名） |
| `OBS_AK` | 你的 AK |
| `OBS_SK` | 你的 SK |
| `OBS_ENDPOINT` | `obs.cn-north-4.myhuaweicloud.com`（按实际区域改）|
| `ALLOWED_ORIGINS` | `https://aiscience2026.github.io` |

注意：
- `OBS_AK` / `OBS_SK` 是敏感信息，**不要提交到 git**，只在控制台填
- `ALLOWED_ORIGINS` 先只放落地页域名，防止别人乱刷你的函数

---

## 五、加 APIG 触发器（5 分钟）

1. 函数详情 →「触发器」标签页 →「创建触发器」
2. **类型**：`API 网关 (APIG)` → 选「通过 APIG 服务开放」
3. 「创建 API」：
   - **API 名称**：`lead-collector-api`
   - **所属分组**：没有就新建一个，如 `source-trace`
   - **请求协议**：HTTPS
4. 「下一步」配安全认证：
   - 认证方式：**None**（公开接口，靠前端校验 + CORS 防滥用）
   - 或选「App 认证」更安全，但落地页要带签名，麻烦。先 None，被刷了再收紧
5. **请求路径**：`/v1/leads`
6. **方法**：`POST`
7. **协议**：`HTTPS`
8. 「下一步」配后端：
   - 后端类型：函数
   - 函数：选你刚建的 `lead-collector`
9. 「完成」→ 「发布 API」（必须发布才生效）

发布后拿到一个调用 URL，形如：
```
https://xxxxxxxx.apig.cn-north-1.huaweicloudapis.com/v1/leads
```

---

## 六、改落地页地址（2 分钟）

1. 打开本地 `index.html`
2. 找到这一行：
   ```js
   const LEAD_ENDPOINT = 'https://YOUR-APIG-ENDPOINT/v1/leads';
   ```
3. 换成控制台给你的真实地址
4. 提交推送：
   ```bash
   cd "D:/self-development/WorkBuddy开发者/人因工程师副业睡后收入/source_trace"
   git add index.html && git commit -m "feat: wire up lead collector endpoint" && git push
   ```
5. 等 1-2 分钟 GitHub Pages 更新

---

## 七、验证（3 分钟）

### 1. 直接测函数

控制台 → 函数详情 →「测试」标签页 → 构造测试事件：

```json
{
  "httpMethod": "POST",
  "headers": {
    "Content-Type": "application/json",
    "Origin": "https://aiscience2026.github.io"
  },
  "body": "{\"email\":\"test@example.com\"}"
}
```

点「测试」，应该返回：
```json
{"statusCode": 200, "body": "{\"ok\": true, \"message\": \"subscribed\", ...}"}
```

### 2. 测完整链路

在 tabbit 打开 `https://aiscience2026.github.io/source-trace/`，填邮箱点订阅，应该原地显示「✓ 订阅成功」。

### 3. 看数据落库

控制台 → OBS → `source-trace-leads` 桶 → `leads/` 目录 → `2026-09-22.jsonl`，下载看内容：

```json
{"ts":"2026-09-22T09:15:30+08:00","email":"test@example.com","ip":"1.2.3.4","ua":"...","referrer":"...","request_id":"a1b2c3d4"}
```

---

## 常见问题

**Q: 测试返回 500，detail 是 "OBS credentials missing"**
A: 环境变量没配对，检查 `OBS_AK` / `OBS_SK` / `OBS_ENDPOINT` 是否都填了。

**Q: 页面提交报 CORS 错误**
A: `ALLOWED_ORIGINS` 没包含你的域名，或函数里 `_cors_headers` 没生效。确认域名没有多余斜杠。

**Q: APIG 返回 401**
A: 建 API 时选了「App 认证」，改成 None，或者重新发布。

**Q: 数据写不进 OBS**
A: 大概率是区域不匹配——函数在 `cn-north-4`，桶建在了别处。也可能桶权限不是「私有」之外的问题，先确认桶名和环境变量一致。

---

## 上线后加固（可选）

- 加简单验证码或 honeypot 字段防机器人
- `ALLOWED_ORIGINS` 收紧，只留落地页域名
- 加每日调用量上限告警
