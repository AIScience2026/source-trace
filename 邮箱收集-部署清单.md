# FunctionGraph 邮箱收集 — 部署清单（SMTP 版）

> 相比 OBS 版：**不用建 OBS 桶、不用拿 AK/SK、不用绑卡充值、不装第三方依赖**
> 每条订阅直接发一封邮件到你邮箱 —— 收件箱就是数据库
> 完成时间：**15 分钟**

---

## 第 1 步：拿 QQ 邮箱授权码（3 分钟）

1. 浏览器打开 https://mail.qq.com → 登录
2. 「设置」→「账号」
3. 找到「POP3/IMAP/SMTP/...」→ IMAP/SMTP 服务 **开启**
4. 按提示发一条短信，然后得到一串 **16 位授权码**
5. 复制保存（**这不是你的 QQ 密码**，是专用密码）

> 用别的邮箱也行：Gmail 要应用专用密码，163 要客户端授权码。都是邮箱设置里单独生成。

---

## 第 2 步：创建函数（5 分钟）

1. 华为云控制台 → 搜索「函数计算 FunctionGraph」
2. 「创建函数」：
   - 类型：**事件函数**
   - 函数名：`lead-collector`
   - 区域：`华北-北京四`（cn-north-4，选离你近的）
   - 运行时：**Python 3.9**
   - 上传方式：代码在线编辑
3. 创建后进函数 →「代码」页，把 `functions/lead_collector/index.py` **整份内容**粘贴进去
4. 点「部署」

**没有第 3 步加依赖了** —— 只用标准库，直接跑。

---

## 第 3 步：配环境变量（2 分钟）

函数 →「配置」→「环境变量」，加这 4 个：

| 键 | 值 | 说明 |
|---|---|---|
| `SMTP_USER` | 你的 QQ 邮箱 | 发件人 |
| `SMTP_PASS` | 16 位授权码 | **不是 QQ 密码** |
| `LEAD_TO` | 要收通知的邮箱 | 可以填 agent 邮箱或任意地址 |
| `ALLOWED_ORIGINS` | `https://aiscience2026.github.io` | 只让你的落地页能调，防乱刷 |

> `SMTP_PASS` 只在控制台填，**不要发我、不要写进任何文件**。

---

## 第 4 步：加 APIG 触发器（5 分钟）

1. 函数 →「触发器」→「创建触发器」
2. 类型：**API 网关 APIG** →「通过 APIG 服务开放」
3. 「新建 API」：
   - 名称：`lead-collector-api`
   - 分组：没有就新建，如 `source-trace`
   - 协议：HTTPS
4. 下一步 → 安全认证：**None**（公开接口；选「App 认证」落地页调不通）
5. 请求：路径 `/v1/leads`，方法 `POST`，协议 HTTPS
6. 下一步 → 后端：选 `lead-collector`
7. 「完成」→ **务必点「发布 API」**，不发布不生效
8. 复制调用地址，形如：
   ```
   https://xxxxxxxx.apig.cn-north-4.huaweicloudapis.com/v1/leads
   ```

---

## 第 5 步：填回落地页并推送（2 分钟）

```bash
cd "D:/self-development/WorkBuddy开发者/人因工程师副业睡后收入/source_trace"
```

打开 `index.html`，把：

```js
const LEAD_ENDPOINT = 'https://YOUR-APIG-ENDPOINT/v1/leads';
```

改成控制台给你的真实地址，然后：

```bash
git add index.html && git commit -m "feat: wire lead endpoint" && git push
```

等 1-2 分钟 GitHub Pages 自动更新。

---

## 验证（2 分钟）

### 1. 先测函数本身

函数详情 →「测试」标签页 → 测试事件粘贴：

```json
{
  "httpMethod": "POST",
  "headers": {
    "Content-Type": "application/json",
    "Origin": "https://aiscience2026.github.io"
  },
  "body": "{\"email\":\"test@example.com\"}"
}
```

点「测试」→ 返回 `{"statusCode": 200, ...}` 且你邮箱收到一封
`[SourceTrace] 新订阅：test@example.com` 就通了。

### 2. 再测线上页面

tabbit 打开 `https://aiscience2026.github.io/source-trace/`，填邮箱点「订阅通知」
→ 原地显示「✓ 订阅成功」，同时邮箱收到邮件。

---

## 常见问题

| 现象 | 原因 / 解法 |
|------|------|
| 返回 500，detail 有 `missing env vars` | `SMTP_USER`/`SMTP_PASS`/`LEAD_TO` 有一个没填 |
| 返回 500，detail 是 SMTP 认证失败 | 填的是 QQ 登录密码，不是 16 位授权码 |
| 返回 500，connection refused / timeout | 换 `SMTP_HOST`/`SMTP_PORT`；QQ 默认 `smtp.qq.com` / `465` 是对的 |
| 返回 401 | APIG 建 API 时选了「App 认证」，改成 None 后**重新发布** |
| 浏览器报 CORS 错 | `ALLOWED_ORIGINS` 没写对，注意 **不要有多余斜杠** |
| 页面显示"服务暂不可用" | 要么 `LEAD_ENDPOINT` 还是占位符，要么函数没部署 |
| 没收到邮件但接口返回成功 | 查垃圾箱；发件人是别人域名容易进垃圾箱 |

---

## 数据在哪

**三条，够用了**：

1. **邮箱收件箱** —— 主库，每封邮件一条完整记录
2. **函数日志** —— FunctionGraph「监控」→「日志」，7 天滚动，兜底
3. 订阅量大了再迁数据库 —— 一天几十封邮件真的能看

## 想加固的话（可选）

- 前端加个 honeypot 隐藏字段挡机器人
- APIG 加「流控」：比如每秒 1 次请求上限，防刷
- `ALLOWED_ORIGINS` 只留你自己的域名（上面已经这么配了）
